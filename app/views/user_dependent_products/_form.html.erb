<%= form_for(@user_dependent_product) do |f| %>
  <% if @user_dependent_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_dependent_product.errors.count, "error") %> prohibited this user_dependent_product from being saved:</h2>

      <ul>
      <% @user_dependent_product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :product_id %><br />
    <%= f.number_field :product_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
