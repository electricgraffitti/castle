<%= (order_product.product_location).capitalize %> 
<%= link_to('Reassign', reassign_order_product_path(id: order_product.id, reassign: 'true'), class: 'green listed_action_link') unless order_product.is_finalized? %>